{
  "name": "twitterclone",
  "version": "1.0.0",
  "description": "twitter clone",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix ./client && npm run build --prefix ./client && npm install --prefix ./server && npm run build --prefix ./server && npm install"
  },
  "author": "",
  "license": "ISC"
}
